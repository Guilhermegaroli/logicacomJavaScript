// Área de um Retângulo

let base,altura,area;

base=parseFloat(prompt("Digita o valor da base :"));

altura=parseFloat(prompt("Digita o valor da altura :"));

area=base*altura;

alert("O valor da área corresponde a : "+area);
